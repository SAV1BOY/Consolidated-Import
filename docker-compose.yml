services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: iluminar_imports
      POSTGRES_USER: iluminar
      POSTGRES_PASSWORD: iluminar_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
